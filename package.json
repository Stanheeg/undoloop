{
  "name": "undoloop-alexa-sim",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check src/engine.js && node --check src/app.js && node --check scripts/serve.mjs",
    "serve": "node scripts/serve.mjs"
  }
}
